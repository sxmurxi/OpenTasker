# OpenClaw TaskBot — Полная документация

## Оглавление

1. [Обзор](#обзор)
2. [Архитектура](#архитектура)
3. [Структура проекта](#структура-проекта)
4. [Управление пользователями](#управление-пользователями)
5. [Система ролей и прав](#система-ролей-и-прав)
6. [Управление задачами](#управление-задачами)
7. [Inline-кнопки (Telegram)](#inline-кнопки-telegram)
8. [Визуализация задач](#визуализация-задач)
9. [Автоматические отчёты и напоминания](#автоматические-отчёты-и-напоминания)
10. [Конфигурация](#конфигурация)
11. [Развёртывание (Ansible)](#развёртывание-ansible)
12. [Ручное развёртывание](#ручное-развёртывание)
13. [Обслуживание и диагностика](#обслуживание-и-диагностика)
14. [FAQ](#faq)

---

## Обзор

**TaskBot** — бот для управления командными задачами через Telegram. Построен как кастомный скилл для платформы OpenClaw.

### Что умеет:
- Создание задач на естественном языке (русский / английский)
- Назначение исполнителей по @username или имени (fuzzy-поиск)
- Дедлайны с автоматическими напоминаниями (за 24ч, за 1ч, при наступлении)
- Ежедневные стендапы и еженедельные обзоры
- Статистика, поиск, фильтрация задач
- Inline-кнопки для быстрых действий с задачами
- Визуализация: графики статусов, нагрузки, приоритетов, трендов
- Работа в личных сообщениях и групповых чатах

### Что НЕ делает:
- Не отвечает на вопросы, не связанные с задачами
- Не выполняет произвольные команды (кроме админа)
- Не реагирует на сообщения от пользователей вне allowlist

---

## Архитектура

```
Telegram → OpenClaw Gateway → Agent (LLM) → team-taskmanager skill → SQLite
                                   ↓
                              OpenClaw Cron → напоминания → Telegram
```

### Компоненты:

| Компонент | Роль |
|-----------|------|
| **OpenClaw Gateway** | WebSocket-сервер, принимает сообщения из Telegram, маршрутизирует к агенту |
| **Telegram Plugin** | Polling-адаптер, получает/отправляет сообщения через Telegram Bot API |
| **Agent (LLM)** | GPT / Claude — интерпретирует естественный язык, вызывает скрипты |
| **team-taskmanager skill** | SKILL.md (инструкции агенту) + Python-скрипты (CRUD задач) |
| **SQLite** | Хранение задач и пользователей в `~/.openclaw/workspace/data/taskmanager.db` |
| **OpenClaw Cron** | Планировщик для напоминаний, стендапов, проверки просроченных задач |

### Принцип работы:

1. Пользователь пишет сообщение в Telegram
2. OpenClaw Gateway получает его через polling
3. Gateway проверяет `allowFrom` — если пользователя нет в списке, сообщение игнорируется
4. Если пользователь в allowlist — сообщение передаётся агенту (LLM)
5. Агент читает SKILL.md, SOUL.md, BOOTSTRAP.md — понимает что он таск-менеджер
6. Агент определяет намерение (создать задачу / показать список / etc.)
7. Агент вызывает соответствующий Python-скрипт через bash
8. Результат (JSON) обрабатывается агентом и форматируется в читаемый ответ
9. Ответ отправляется обратно в Telegram

---

## Структура проекта

```
~/team-taskmanager/
├── DOCS.md                              ← этот файл
├── SKILL.md                             # Инструкции для агента (главный файл скилла)
├── README.md                            # Краткая документация
│
├── scripts/                             # Python CLI-скрипты (stdlib only)
│   ├── init_db.py                       # Инициализация SQLite БД
│   ├── tasks.py                         # CRUD задач (529 строк)
│   ├── resolve_user.py                  # Регистрация + fuzzy-поиск юзеров (244 строки)
│   ├── reminders.py                     # Проверка дедлайнов, overdue (166 строк)
│   ├── standup.py                       # Ежедневный отчёт (189 строк)
│   ├── weekly_review.py                 # Еженедельный обзор (210 строк)
│   └── visualize.py                     # Генерация PNG-графиков (348 строк)
│
├── references/                          # Справочные документы для агента
│   ├── task-format.md                   # Спецификация формата задач
│   └── user-resolution.md              # Правила резолвинга пользователей
│
├── assets/templates/
│   └── TASKS_TEMPLATE.md               # Шаблоны отчётов
│
├── config/
│   └── config.json                      # Настройки скилла (timezone, thresholds)
│
└── deploy/                              # Ansible-деплой
    ├── ansible.cfg
    ├── playbook.yml
    ├── .gitignore
    ├── inventories/
    │   └── hosts.yml                    # Инвентарь серверов
    ├── vars/
    │   ├── secrets.yml                  # Секреты (НЕ коммитить!)
    │   └── secrets.yml.example          # Пример
    └── roles/
        └── openclaw-taskbot/
            ├── defaults/main.yml        # Дефолтные переменные
            ├── tasks/main.yml           # Ansible таски
            ├── handlers/main.yml        # Хэндлеры (restart)
            ├── templates/               # Jinja2-шаблоны конфигов
            │   ├── openclaw.json.j2
            │   ├── env.j2
            │   ├── BOOTSTRAP.md.j2
            │   ├── SOUL.md.j2
            │   ├── IDENTITY.md.j2
            │   ├── USER.md.j2
            │   ├── SKILL.md.j2
            │   └── config.json.j2
            └── files/
                └── skill/               # Копия скриптов и справочников
                    ├── scripts/*.py
                    ├── references/*.md
                    ├── assets/templates/*.md
                    └── README.md
```

### Файлы на сервере (после деплоя):

```
~/.openclaw/
├── openclaw.json                        # Главный конфиг OpenClaw
├── .env                                 # API-ключи
└── workspace/
    ├── BOOTSTRAP.md                     # Инструкции при старте агента
    ├── SOUL.md                          # Правила поведения + роли
    ├── IDENTITY.md                      # Личность бота
    ├── USER.md                          # Список пользователей
    ├── data/
    │   ├── taskmanager.db               # SQLite база данных
    │   └── charts/                      # PNG-графики визуализации
    └── skills/
        └── team-taskmanager/            # Скилл (аналогичная структура)
```

---

## Управление пользователями

### Где хранятся списки доступа

Доступ контролируется в **двух местах**:

#### 1. `~/.openclaw/openclaw.json` — канальный allowlist (главный фильтр)

```json
"channels": {
  "telegram": {
    "allowFrom": ["sadindeed", "Eshanchik", "SegovChik"]
  }
}
```

Пользователи НЕ в этом списке — бот их **полностью игнорирует** (даже не тратит токены LLM).

**Как редактировать:**
```bash
# С сервера:
openclaw config set "channels.telegram.allowFrom" '["sadindeed","Eshanchik","SegovChik","НовыйUser"]'
openclaw gateway restart

# Или через Ansible — отредактировать deploy/roles/openclaw-taskbot/defaults/main.yml:
allowed_users:
  - "sadindeed"
  - "Eshanchik"
  - "SegovChik"
  - "НовыйUser"
# Затем: ansible-playbook playbook.yml
```

#### 2. `~/.openclaw/workspace/USER.md` — роли (админ / обычный)

```markdown
## Admin
- **Name:** @sadindeed
- **Role:** Admin — full access to all bot functions

## Users
- **@Eshanchik** — regular user, task management only
- **@SegovChik** — regular user, task management only
```

**Как редактировать:**
Вручную на сервере (`nano ~/.openclaw/workspace/USER.md`) или через Ansible-переменные:

```yaml
# deploy/roles/openclaw-taskbot/defaults/main.yml
admin_username: "sadindeed"
regular_users:
  - username: "Eshanchik"
    note: "regular user, task management only"
  - username: "НовыйUser"
    note: "regular user, task management only"
```

### Добавление нового пользователя (чеклист)

1. Добавить username в `allowed_users` (defaults/main.yml или openclaw.json)
2. Добавить в `regular_users` (defaults/main.yml или USER.md)
3. `openclaw gateway restart` (или `ansible-playbook playbook.yml`)

### Удаление пользователя

1. Убрать из `allowed_users` и `regular_users`
2. Перезапустить gateway

---

## Система ролей и прав

### Админ (@sadindeed)

| Действие | Доступ |
|----------|--------|
| Создание задач | YES |
| Просмотр любых задач | YES |
| Редактирование/отмена любых задач | YES |
| Конфигурация бота | YES |
| Настройка cron | YES |
| Диагностика и системные команды | YES |
| Произвольные инструкции | YES |

### Обычный пользователь

| Действие | Доступ |
|----------|--------|
| Создание задач (себе и другим) | YES |
| Просмотр своих задач / по ID | YES |
| Отметить свою задачу done/in_progress | YES |
| Поиск, статистика, стендап | YES |
| Продление дедлайна своей задачи | YES |
| Отмена/редактирование ЧУЖИХ задач | NO |
| Конфигурация, cron, системные команды | NO |
| Сообщения явно не о задачах (погода, анекдоты, код) | NO (вежливый отказ) |

---

## Управление задачами

### Создание задач

Примеры сообщений (бот понимает естественный язык):

```
Подготовить презентацию для клиента @ivan до пятницы
Напомни @sergey обновить документацию, срочно
Задача: ревью PR #42, без дедлайна
Сделать отчёт к 15 февраля 18:00
```

Бот извлекает: описание, исполнитель, дедлайн, приоритет.

### Просмотр

```
мои задачи
задачи @ivan
покажи задачу #15
все просроченные
статистика
```

### Управление статусом

```
задача 15 готова        → done
начал задачу 15         → in_progress
отмени задачу 15        → cancelled
продли дедлайн #15 до среды
```

### Приоритеты

| Приоритет | Эмодзи | Ключевые слова |
|-----------|--------|----------------|
| high | 🔴 | срочно, urgent, приоритет высокий |
| medium | 🟡 | (по умолчанию) |
| low | 🟢 | low priority, не срочно |

### Дедлайны (парсинг дат)

Бот понимает:
- **Русский:** до пятницы, через 3 дня, к среде, до конца недели, завтра к 18:00, 15 февраля
- **English:** by Friday, in 3 days, next Monday, tomorrow by 6pm, Feb 15

---

## Inline-кнопки (Telegram)

Бот отправляет inline-кнопки для быстрых действий прямо в чате.

### После создания задачи:

```
✅ Задача #12 создана
📝 Подготовить презентацию
👤 @ivan
📅 15 фев

[▶️ В работу] [✅ Готово]
[✏️ Изменить] [❌ Отменить]
```

### При просмотре списка задач:

```
📋 Задачи для @username (3 активных)
...

[📊 Статистика] [⏰ Просроченные]
[📈 Визуализация]
```

### При просмотре одной задачи:

```
📋 Задача #12
...

[✅ Готово] [▶️ В работу]
[📅 Продлить] [❌ Отменить]
```

Нажатие на кнопку отправляет callback, бот выполняет действие и подтверждает результат.

### Технический формат

Кнопки передаются через `openclaw message send --buttons` в виде JSON-массива строк (rows), каждая строка — массив кнопок:
```json
[[{"text":"✅ Готово","callback_data":"done_12"},{"text":"▶️ В работу","callback_data":"start_12"}]]
```

---

## Визуализация задач

Бот генерирует PNG-графики по запросу. Требуется `python3-matplotlib`.

### Доступные графики

| Запрос | Тип | Описание |
|--------|-----|----------|
| `визуализация` / `дашборд` | Все 4 графика | Полный обзор |
| `покажи статусы` | Круговая диаграмма | Распределение по статусам (todo/in_progress/done/cancelled/overdue) |
| `нагрузка команды` | Горизонтальная столбчатая | Задачи на каждого исполнителя |
| `приоритеты` | Столбчатая | Активные задачи по приоритетам (high/medium/low) |
| `тренд` / `динамика` | Линейный график | Создание vs выполнение задач по дням за N недель |

### Примеры использования

```
визуализация          → все 4 графика
покажи статусы        → круговая диаграмма
нагрузка команды      → столбчатая по исполнителям
тренд за 8 недель     → линейный график за 8 недель
```

### CLI

```bash
python3 scripts/visualize.py status [--chat-id <id>]
python3 scripts/visualize.py workload [--chat-id <id>]
python3 scripts/visualize.py priority [--chat-id <id>]
python3 scripts/visualize.py trend [--chat-id <id>] [--weeks 4]
python3 scripts/visualize.py dashboard [--chat-id <id>]
```

Графики сохраняются в `~/.openclaw/workspace/data/charts/` и отправляются в Telegram через `--media`.

---

## Автоматические отчёты и напоминания

### Напоминания по задачам

При создании задачи с дедлайном автоматически создаются cron-джобы:

| Когда | Сообщение |
|-------|-----------|
| За 24 часа | ⏰ Напоминание: задача #ID — дедлайн через 24 часа |
| За 1 час | 🔔 Задача #ID — дедлайн через 1 час! |
| При наступлении | 🚨 Дедлайн наступил! Задача #ID — обновите статус |

При выполнении/отмене задачи — cron-джобы удаляются.
При продлении дедлайна — пересоздаются на новую дату.

### Ежедневный стендап

По будням в 9:30 (настраивается):
```
📊 Ежедневный отчёт — 10.02.2026

🔴 Просрочено (2):
  #12 Презентация — @ivan (просрочено на 2 дня)

⏰ Дедлайн сегодня (1):
  #15 Документация — @anna (до 18:00)

🔄 В работе (3):
  🔴 #18 Ревью PR — @ivan
  🟡 #20 Тесты — @sergey

📈 Выполнено вчера: 4 задачи
```

### Еженедельный обзор

По понедельникам в 10:00:
- Статистика за неделю (создано / выполнено / отменено)
- Сравнение с прошлой неделей (тренды ⬆️⬇️)
- Топ участников по выполненным задачам
- Completion rate
- Автоматическая архивация старых задач (по желанию)

---

## Конфигурация

### Главный конфиг: `~/.openclaw/openclaw.json`

Ключевые секции:

```jsonc
{
  "channels.telegram.allowFrom": ["user1", "user2"],  // allowlist
  "channels.telegram.dmPolicy": "allowlist",           // фильтрация DM
  "channels.telegram.groupPolicy": "allowlist",        // фильтрация групп
  "agents.defaults.model.primary": "openai/gpt-5.1-codex",  // LLM модель
  "skills.entries.team-taskmanager.enabled": true       // скилл включён
}
```

### Конфиг скилла: `config/config.json`

```json
{
  "timezone": "Europe/Kiev",
  "language": "auto",
  "default_priority": "medium",
  "fuzzy_threshold": 0.6,
  "max_suggestions": 5,
  "standup": { "enabled": true, "cron": "30 9 * * 1-5" },
  "weekly_review": { "enabled": true, "cron": "0 10 * * 1" },
  "overdue_check": { "enabled": true, "cron": "0 */4 * * *" }
}
```

### Переменные Ansible: `deploy/roles/openclaw-taskbot/defaults/main.yml`

Все настройки параметризованы. Меняешь переменные — запускаешь плейбук — всё обновляется.

---

## Развёртывание (Ansible)

### Требования
- Ansible 2.14+ на локальной машине
- SSH-доступ к серверу (root)
- Debian/Ubuntu сервер

### Первый деплой

```bash
cd ~/team-taskmanager/deploy

# 1. Настроить инвентарь
#    Отредактировать inventories/hosts.yml — указать IP сервера

# 2. Настроить секреты
cp vars/secrets.yml.example vars/secrets.yml
#    Вписать telegram_bot_token и openai_api_key

# 3. Настроить пользователей
#    Отредактировать roles/openclaw-taskbot/defaults/main.yml
#    → allowed_users, regular_users, admin_username

# 4. Запустить
ansible-playbook playbook.yml
```

### Что делает плейбук:
1. Устанавливает git, curl, python3-matplotlib, Node.js 22
2. Устанавливает OpenClaw глобально (`npm install -g openclaw`)
3. Создаёт структуру директорий
4. Генерирует `openclaw.json` из шаблона (с allowlist, токенами, моделью)
5. Деплоит `.env` с API-ключами
6. Деплоит workspace-файлы (BOOTSTRAP, SOUL, IDENTITY, USER)
7. Копирует скрипты скилла и шаблоны
8. Деплоит SKILL.md и config.json (с подстановкой admin_username)
9. Инициализирует SQLite-базу
10. Включает systemd linger
11. Устанавливает и запускает gateway как systemd-сервис
12. Проверяет здоровье (health check)

### Обновление конфигурации

```bash
# Изменить переменные и перезапустить:
ansible-playbook playbook.yml
```

### Шифрование секретов

```bash
ansible-vault encrypt vars/secrets.yml
# При запуске:
ansible-playbook playbook.yml --ask-vault-pass
```

---

## Ручное развёртывание

Если Ansible не подходит:

```bash
# 1. На сервере установить Node.js + OpenClaw:
curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
apt install -y nodejs git
npm install -g openclaw

# 2. Настроить OpenClaw:
openclaw onboard --non-interactive --accept-risk \
  --auth-choice openai-api-key \
  --openai-api-key "YOUR_KEY" \
  --workspace ~/.openclaw/workspace \
  --skip-channels --skip-ui --skip-health \
  --gateway-auth token --gateway-bind loopback

# 3. Включить Telegram:
openclaw plugins enable telegram
openclaw channels add --channel telegram \
  --token "YOUR_BOT_TOKEN" --name "TaskManagerBot"

# 4. Настроить доступ:
openclaw config set channels.telegram.dmPolicy allowlist
openclaw config set channels.telegram.groupPolicy allowlist
openclaw config set "channels.telegram.allowFrom" '["user1","user2"]'

# 5. Включить скилл:
openclaw config set skills.entries.team-taskmanager.enabled true

# 6. Скопировать файлы скилла:
scp -r ~/team-taskmanager/{scripts,references,assets,config,SKILL.md,README.md} \
  root@SERVER:~/.openclaw/workspace/skills/team-taskmanager/

# 7. Скопировать workspace-файлы (BOOTSTRAP.md, SOUL.md, etc.)

# 8. Инициализировать БД:
python3 ~/.openclaw/workspace/skills/team-taskmanager/scripts/init_db.py

# 9. Запустить:
chmod 700 ~/.openclaw
loginctl enable-linger root
openclaw gateway install
openclaw gateway start
```

---

## Обслуживание и диагностика

### Полезные команды (выполнять на сервере)

```bash
# Статус бота
openclaw health
openclaw channels status --json
openclaw gateway status --json

# Логи
openclaw logs --follow

# Перезапуск
openclaw gateway restart

# Проверка здоровья
openclaw doctor

# Список скиллов
openclaw skills list --json | python3 -c "import sys,json; [print(s['name'], s['eligible']) for s in json.load(sys.stdin)['skills']]"

# Ручной тест скриптов
python3 ~/.openclaw/workspace/skills/team-taskmanager/scripts/tasks.py stats --chat-id -100123
python3 ~/.openclaw/workspace/skills/team-taskmanager/scripts/resolve_user.py list
```

### База данных

```bash
# Просмотр содержимого
sqlite3 ~/.openclaw/workspace/data/taskmanager.db

# Полезные SQL-запросы:
.tables
SELECT * FROM tasks WHERE status != 'done' ORDER BY deadline;
SELECT * FROM users;
SELECT COUNT(*), status FROM tasks GROUP BY status;
```

### Бэкап

```bash
# Бэкап БД
cp ~/.openclaw/workspace/data/taskmanager.db ~/taskmanager-backup-$(date +%Y%m%d).db

# Бэкап всего конфига
tar czf ~/openclaw-backup-$(date +%Y%m%d).tar.gz ~/.openclaw/
```

---

## FAQ

**Q: Бот не отвечает на сообщения?**
1. Проверь `openclaw health` — Telegram должен быть "ok"
2. Проверь `allowFrom` — username пользователя в списке?
3. Проверь логи: `openclaw logs --follow`

**Q: Бот отвечает на нерелевантные вопросы?**
Проверь что SOUL.md и BOOTSTRAP.md на месте и содержат ограничения.
Сбрось сессии: `rm ~/.openclaw/agents/main/sessions/sessions.json && openclaw gateway restart`

**Q: Бот отклоняет ВСЕ сообщения (даже задачи)?**
Промпты слишком строгие. SOUL.md должен содержать правило:
"DEFAULT BEHAVIOR: Treat the message as task-related and try to help."
Бот должен отклонять ТОЛЬКО явно нерелевантные запросы (погода, анекдоты, код).
Сбрось сессии после исправления: `rm ~/.openclaw/agents/main/sessions/sessions.json && openclaw gateway restart`

**Q: Как добавить пользователя?**
См. [Управление пользователями](#управление-пользователями)

**Q: Как сменить LLM-модель?**
```bash
openclaw config set agents.defaults.model.primary "anthropic/claude-sonnet-4-5-20250929"
openclaw gateway restart
```

**Q: Как посмотреть расход токенов?**
```bash
openclaw gateway usage-cost
```

**Q: Бот упал после перезагрузки сервера?**
```bash
loginctl enable-linger root
openclaw gateway install --force
openclaw gateway start
```
